export function Button({className='',variant,children,...props}){return <button className={`inline-flex items-center justify-center rounded-md px-4 py-2 font-semibold transition ${className}`} {...props}>{children}</button>}
